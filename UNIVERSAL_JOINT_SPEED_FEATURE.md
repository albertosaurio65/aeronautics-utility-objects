# 万向节变速功能实现说明

## 功能概述

为万向节（Universal Joint）添加了类似机械动力变速器的变速传输功能，允许玩家通过GUI调整连接两端的速度倍率。

## 功能特性

### 1. 速度倍率范围
- **范围**: -4.0x 到 +4.0x
- **默认值**: 1.0x（无变速）
- **中间值**: 0x（停止传动）
- **负值**: 反向旋转
- **正值**: 正向旋转

### 2. GUI界面
- **打开方式**: Shift + 右键点击已连接的万向节
- **界面布局**:
  - 标题显示当前方块名称
  - 速度倍率滑块（-4x 到 +4x）
  - 当前速度倍率数值显示
  - 确认按钮关闭界面

### 3. 滑块操作
- **鼠标拖动**: 直接拖动滑块调整速度
- **鼠标滚轮**: 
  - 普通滚动: 每次调整 0.1x
  - Shift + 滚动: 每次调整 0.5x
- **刻度标记**: 显示 -4x, -2x, 0x, 2x, 4x 五个主要刻度

### 4. 动力传输
- 速度倍率实时应用到连接的另一端
- 支持正向和反向传动
- 0x时完全停止传动
- 倍率变化会触发动力网络刷新

## 实现的文件

### 新增文件
1. **UniversalJointMenu.java** - GUI菜单容器
2. **UniversalJointScreen.java** - GUI渲染界面
3. **SetJointSpeedRatioPayload.java** - 网络通信包

### 修改文件
1. **UniversalJointBlockEntity.java**
   - 添加 `speedRatio` 字段存储速度倍率
   - 实现 `MenuProvider` 接口
   - 添加速度倍率的getter/setter方法
   - 修改 `propagateRotationTo()` 方法应用速度倍率
   - 添加NBT读写支持

2. **UniversalJointBlock.java**
   - 修改 `useWithoutItem()` 方法
   - Shift + 右键有连接时打开GUI
   - Shift + 右键无连接时断开连接

3. **ModMenuTypes.java**
   - 注册 `UNIVERSAL_JOINT` 菜单类型

4. **AeroUniversalJointClient.java**
   - 注册 `UniversalJointScreen` 客户端界面

5. **AeroUniversalJointNetwork.java**
   - 注册 `SetJointSpeedRatioPayload` 网络包

6. **语言文件**
   - en_us.json: 添加 "Speed Ratio:" 翻译
   - zh_cn.json: 添加 "速度倍率：" 翻译

## 使用方法

### 基本操作
1. 使用连接杆连接两个万向节
2. Shift + 右键点击任意一个已连接的万向节
3. 在GUI中拖动滑块或使用滚轮调整速度倍率
4. 点击确认按钮或按ESC关闭界面

### 速度倍率说明
- **1.0x**: 默认值，1:1传动
- **2.0x**: 输出端速度是输入端的2倍
- **0.5x**: 输出端速度是输入端的一半
- **-1.0x**: 反向传动，速度相同
- **0x**: 停止传动
- **4.0x**: 最大加速，输出端速度是输入端的4倍
- **-4.0x**: 最大反向加速

### 应用场景
- **加速传动**: 设置正值倍率（如2x, 3x, 4x）
- **减速传动**: 设置小于1的正值（如0.5x, 0.25x）
- **反向传动**: 设置负值倍率（如-1x, -2x）
- **停止传动**: 设置为0x
- **精确控制**: 使用滚轮微调（0.1x步进）

## 技术细节

### 数据同步
- 使用 `ContainerData` 自动同步速度倍率
- float值转换为int传输（乘以100保留两位小数）
- 客户端修改通过网络包发送到服务端

### 动力传播
- `propagateRotationTo()` 方法返回速度倍率
- Create模组的动力系统会自动应用这个倍率
- 倍率变化时调用 `refreshKinetics()` 刷新动力网络

### 数据持久化
- 速度倍率保存在NBT标签 "SpeedRatio" 中
- 支持世界保存和加载
- 客户端和服务端数据同步

## 注意事项

1. **连接要求**: 必须先连接两个万向节才能打开GUI
2. **材质匹配**: 两端万向节材质必须相同（安山或黄铜）
3. **应力计算**: 速度倍率会影响应力传递
4. **网络刷新**: 修改倍率会触发整个动力网络重新计算

## 兼容性

- 完全兼容现有的万向节连接系统
- 不影响未设置倍率的万向节（默认1.0x）
- 支持安山和黄铜两种材质的万向节
- 与Create模组的动力系统完全兼容
